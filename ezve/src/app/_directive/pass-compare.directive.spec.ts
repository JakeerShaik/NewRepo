import { PassCompareDirective } from './pass-compare.directive';

describe('PassCompareDirective', () => {
  it('should create an instance', () => {
    const directive = new PassCompareDirective();
    expect(directive).toBeTruthy();
  });
});
